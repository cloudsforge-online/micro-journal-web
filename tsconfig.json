{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "jsxImportSource": "react",

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "resolveJsonModule": true,

    // Relative imports carry their real '.ts'/'.tsx' extension so that tsc, Vite and
    // `node --import tsx` all resolve one spelling. Nothing is emitted from here — Vite builds —
    // so the extension never has to be rewritten.
    "allowImportingTsExtensions": true,
    "noEmit": true,

    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "useDefineForClassFields": true
  },
  // `scripts` is in here, unlike every sibling repository, because on this surface the scripts are
  // not tooling — `scripts/prerender.ts` writes the HTML that a crawler actually reads. A build
  // step that composes production markup is production code and is typechecked as such.
  "include": ["src", "test", "scripts", "vite.config.ts"]
}
